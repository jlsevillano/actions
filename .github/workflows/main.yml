name: Test


on: [push]

jobs:
  test-build:
    runs-on: ubuntu-latest

    steps:
    # - name: LS
    #   run: ls -la

      - name: LS
        run: |
          ls -la
          echo "Hello World" >> hola.txt
          cat hola.txt
  

    # - name: Checkout
    #   uses: actions/checkout@v4.1.7

    # - name: Install dependencies
    #   run: npm install

    # - name: Build
    #   run: npm run build

    # - name: Test
    #   run: npm test